enum LabelProperty {
  ObligationToProvideInformation
  Picture
  Usp
  TextChapter
  ParkingSpecification
  MonumentSpecification
  InsideSpecification
  HousingOptions
  SpecialTags
  PropertyRights
  HomeOwnerAssociation
  LocationGoodToKnow
  Location
  CadastreMap
  MediaLink
  Tag
  TankInspection
  PollutionInspection
  MaintenanceInspection
  Cost
}

type Label {
  id: ID!
  icon: String
  text: String!
  property: LabelProperty!
}

input LabelInput {
  pimId: ID!
  icon: String
  text: String!
  property: LabelProperty!
}

extend type Mutation {
  addLabel(input: LabelInput!): Label! @auth
  removeLabel(id: ID!): Boolean! @auth
}

extend type Query {
  getLabels(pimId: ID!, properties: [LabelProperty!]): [Label!] @auth
}
