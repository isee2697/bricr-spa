type Me {
    firstName: String!
    lastName: String!
    email: String!
    avatar: String!
  }

type Query {
  me: Me
}

type LoginResponse {
  accessToken: String!
  refreshToken: String!
}

type Mutation {
  login(login: String!, password: String!): LoginResponse
}