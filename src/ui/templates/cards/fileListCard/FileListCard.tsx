import React from 'react';
import { Card } from '@material-ui/core';

import { FileListCardProps } from 'ui/templates/cards/fileListCard/FileListCard.types';
import { CardWithBody } from 'ui/templates/cards/cardWithBody/CardWithBody';

export const FileListCard = ({ disableUpload }: FileListCardProps) => {
  return <CardWithBody>test</CardWithBody>;
};
