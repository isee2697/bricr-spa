import React from 'react';
import { Card } from '@material-ui/core';

import { CardWithBody } from 'ui/templates/cards/cardWithBody/CardWithBody';

import { CardWithFileListProps } from './CardWithFileList.types';

export const CardWithFileList = ({ disableUpload }: CardWithFileListProps) => {
  return <CardWithBody>test</CardWithBody>;
};
