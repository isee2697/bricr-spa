import React from 'react';
import { Card } from '@material-ui/core';

import { FileListCardProps } from './FileListCard.types';

export const FileListCard = ({ disableUpload }: FileListCardProps) => {
  return <Card>test</Card>;
};
