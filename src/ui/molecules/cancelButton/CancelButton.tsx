import React from 'react';

import { CancelButtonProps } from './CancelButton.types';
import * as S from './CancelButton.styles';

export const CancelButton = (props: CancelButtonProps) => <S.CancelButton {...props} />;
