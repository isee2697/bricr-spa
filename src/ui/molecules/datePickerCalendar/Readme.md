Example of DatePickerCalendar

```jsx harmony
import { DateTime } from 'luxon';

import { DatePickerCalendar } from './DatePickerCalendar';

<DatePickerCalendar currentDate={DateTime.local()} onChangeDate={() => {}} />
```