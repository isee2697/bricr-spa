Example of EmptyState component

```jsx harmony
import { EmptyState } from './EmptyState';
import { Typography } from 'ui/atoms';

<EmptyState>
  <Typography variant="h3">You don’t have any added properties yet.</Typography>
  <Typography variant="h3">To change that, click the “Add property” button at the top of the screen.</Typography>
</EmptyState>;
```
