export type Interest = {
  groundInterest?: string;
  buildingInterest?: string;
  interestDays?: string;
  suspensiveConditions?: string;
};

export type InterestForm = {
  groundInterest?: string;
  buildingInterest?: string;
  interestDays?: string;
  suspensiveConditions?: string;
};
