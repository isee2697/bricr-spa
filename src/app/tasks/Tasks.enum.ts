export enum TaskPriority {
  high = 'high',
  medium = 'medium',
  low = 'low',
}

export enum TaskStatus {
  todo = 'todo',
  inProgress = 'inprogress',
  blocked = 'blocked',
  done = 'done',
}

export enum TaskLabel {
  followUp = 'followup',
  business = 'business',
  private = 'private',
}
