import React from 'react';

import { Grid, Box, Typography, Button, IconButton } from 'ui/atoms';

export const Header = () => {
  return (
    <Grid item xs={12} justify="space-between">
      <Box display="flex" alignItems="center"></Box>
    </Grid>
  );
};
